"""Provider name constants shared across the analysis server."""

PROVIDER_CLAUDE = "claude"
PROVIDER_GEMINI = "gemini"
PROVIDER_LMSTUDIO = "lmstudio"
PROVIDER_HERMES = "hermes"
PROVIDER_MOCK = "mock"

ALL_PROVIDERS = (
    PROVIDER_CLAUDE,
    PROVIDER_GEMINI,
    PROVIDER_LMSTUDIO,
    PROVIDER_HERMES,
    PROVIDER_MOCK,
)
